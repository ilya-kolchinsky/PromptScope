# This file is maintained for backwards compatibility
# For development, use: pip install -e ".[all]"
# For specific providers, use: pip install -e ".[anthropic]" or ".[openai]"

# Core dependencies (always installed)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.2
python-dotenv>=1.0.1
httpx>=0.27.0

# Optional dependencies (uncomment as needed)
# anthropic>=0.39.0
# openai>=1.54.3
